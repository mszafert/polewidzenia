<div class="drawer-overlay">
  <div class="drawer">
    <a href="#" class="close"><div class="i-mdi-close"></div></a>
    <slot />
  </div>
</div>

<style>
  .drawer-overlay {
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease-in-out, visibility 0.3s ease-in-out;
    @apply bg-black/20 inset-0 fixed z-1 backdrop-blur-xs;
  }

  .drawer-overlay.open {
    opacity: 1;
    visibility: visible;
  }


  .drawer {
    transform: translateX(-100%);
    transition: transform 0.3s ease-in-out;
    @apply c-black p-15 bg-white h-full md:max-w-xl relative shadow-2xl;
  }

  .drawer.open {
    transform: translateX(0);
  }

  .close {
    @apply opacity-20 transition-opacity hover:opacity-80 text-5xl right-5 top-5 absolute;
  }
</style>
